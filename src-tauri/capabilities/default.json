{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local application window and repository folder picker",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
